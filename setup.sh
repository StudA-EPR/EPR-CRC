#!/bin/sh

#
# Place all commands needed to set up the production environment here.
#

# Sets an environment variable with path to the location where camera
# snapshots are saved to and read from.
export SNAPSHOTS_DIR=/www/snapshots
